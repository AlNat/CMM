int main ( void ) {

	/* */


    /* Комментарий */

	out ( var ); /* Вывод слова */

	int a = 3;  	/* Целочисленные переменные */
	double d = 2.2; 	/* Дробные переменные. Обязтальна полная инициализация */

	/* Арифметика */

    out ( a );

	a = 12;
	out ( a );

	a = 3 + 2;
	out ( a );

	a = 12;
	a = a + 4;
	a = a / 8;
	a = a * 3;
	a = a - 2;
	out ( a );

	out ( d );

	d = d + 0.8;
	d = d / 3.0;
	d = d * 10.0;
	d = d - 5.4;
	out ( d );

	d = 6.012;
	out ( d );


	/* Условные выражения */

    a = 4;
    out ( if after_a = 4 );

	if ( a == 4 ) {
		a = 12;
		out ( a );
	}

   	if ( a == 4 ) {
   		a = -5;
   		out ( a );
   	}

   	a = 3;
   	if ( a == 3 ) {
        a = -18;
       	out ( a );
    }

    /* Циклы */

    out ( endl ); /* Вывод перевода строки */
	int c = 1;
	while ( c < 5 ) {
	    c = c + 1;
	}
	out ( c );

    out ( \n );
	int v = 0;
	double u = 0.5;
	for ( v; v < 4; 1 ) { /* Последнее число совершает не i++ а ++i => пре-инкермент, а не пост-инкримент */
	    out ( u );
	    u = u + 0.5;
	}

	return 0;
}